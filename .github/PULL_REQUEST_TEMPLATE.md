## What & why

<!-- What does this change, and what problem does it solve? Link an issue if there is one. -->

## Checklist

- [ ] Tests added/updated for the behavior changed
- [ ] `uv run ruff check . && uv run ruff format --check . && uv run mypy reovault` pass
- [ ] `uv run pytest` passes
- [ ] Docs updated if this changes user-facing behavior (README, `docs/`, config)
- [ ] Reolink-related behavior is based on verified upstream docs or real device testing, not assumption (see `CLAUDE.md`)
